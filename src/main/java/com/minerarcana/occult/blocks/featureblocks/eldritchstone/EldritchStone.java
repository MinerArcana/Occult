package com.minerarcana.occult.blocks.featureblocks.eldritchstone;

import net.minecraft.block.Block;
import net.minecraft.util.math.BlockPos;

public class EldritchStone extends Block {

    public EldritchStone(Properties properties) {
        super(properties);
    }


    private void stoneGrowth(String name, BlockPos pos, Pressure pressure)
    {
        /*BlockPos up = pos.up();
        IPressure stored = pressure.getPressure(name, pressure);
        if(up == Blocks.AIR.getDefaultState() && stored == 25){
*/
        }

    }





